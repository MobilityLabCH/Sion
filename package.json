{
  "name": "sion-mobility-simulator",
  "private": true,
  "workspaces": ["apps/web", "apps/worker"],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:worker": "npm run dev --workspace=apps/worker",
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:worker\"",
    "build:web": "npm run build --workspace=apps/web",
    "test:worker": "npm run test --workspace=apps/worker",
    "deploy:worker": "npm run deploy --workspace=apps/worker",
    "deploy:web": "npm run deploy --workspace=apps/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
