/**
 * ODSimulator.tsx — stub
 * Le composant est intégré directement dans ODPage.tsx
 * Ce fichier existe uniquement pour éviter les erreurs d'import résiduel.
 */

// Composant vide — toute la logique est dans pages/ODPage.tsx
export default function ODSimulator() {
  return null;
}
